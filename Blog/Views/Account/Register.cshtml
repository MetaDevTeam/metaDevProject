@model Blog.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";
}
<div class="main">
    <div class="content">
        <div class="inner">
            @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "compact", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h3>@ViewBag.Title</h3>
                <h4>Create a new account</h4>
                        <hr />
                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        <p class="input">
                            @Html.LabelFor(m => m.Email, new { @class = "col-sm-4 control-label" })
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                        </p>
                        <p class="input required">
                            @Html.LabelFor(m => m.FullName, new { @class = "col-sm-4 control-label" })
                            @Html.TextBoxFor(m => m.FullName, new { @class = "form-control" })
                        </p>
                        <p class="input required">
                            @Html.LabelFor(m => m.Password)
                            @Html.PasswordFor(m => m.Password)                           
                        </p>
                        <p class="input required">
                            @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-sm-4 control-label" })
                            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
                        </p>
                        <div class="buttons">
                            <input type="submit" value="Register" class="button"/>
                        </div>
            }
        </div>
    </div>
</div>
            @section Scripts {
                @Scripts.Render("~/bundles/jqueryval")
            }
